import { Link } from 'react-router-dom';

const CORE = [
  ['AD-01', 'Отчёт по аудиту', '20 секций: от резюме для владельца до лога версий, каждая цифра с меткой уверенности'],
  ['AD-02', 'Таблица аудита', '52+ метрики: факт · зона 🔴🟡🟢 · эталон · разрыв · плейбук'],
  ['AD-03', 'Реестр болей', 'Сгруппирован по причинам, у каждой боли — доказательство'],
  ['AD-04', 'Экономика разрыва', 'Недополученный оборот по рычагам, цепная модель, консервативный сценарий'],
  ['AD-05', 'Карта scope', 'Какие плейбуки активированы и почему; отдельно — что НЕ вошло'],
  ['AD-06', 'Запрос доступов и вопросов', 'Что нужно, зачем, у кого — привязано к секциям отчёта'],
  ['AD-07', 'Резюме для владельца', '1 страница: 5 находок и одно решение'],
];

const APPS = [
  ['AD-08', 'Карта сайта', 'дерево с вердиктами: работает / лишний / не хватает'],
  ['AD-09', 'Постраничный разбор', '15 типов страниц со скриншотами, мобильная отдельно'],
  ['AD-10', 'Технический бэклог', 'дефекты по соотношению эффект/стоимость'],
  ['AD-11', 'Конкурентный бенчмарк', 'матрица против 5 конкурентов + white space'],
  ['AD-12', 'Цена в канале', 'разброс цен по площадкам и продавцам'],
  ['AD-13', 'Baseline-лист', '8 рычагов выручки на дату старта — точка отсчёта целей'],
  ['AD-14', 'Лог версий', 'что получено, что раскроет следующий доступ'],
  ['AD-15', 'Итоговая презентация', 'диагноз → цена бездействия → план с бюджетом, сроками и DoD'],
  ['AD-16', 'Health Score', 'зрелость 18 доменов + одна цифра из 100'],
  ['AD-17', 'ЕКП — рабочая книга', 'движок анализа: каждый ваш ответ со скорингом и роутингом'],
  ['AD-18', 'Гант программы', 'план работ с фазами, RACI и критическим путём'],
];

export default function DeliverablesPage() {
  return (
    <div className="container" style={{ padding: '30px 20px 80px', maxWidth: 760 }}>
      <Link to="/" className="mono" style={{ fontSize: 12, color: 'var(--muted)', textDecoration: 'none' }}>← На главную</Link>
      <p className="eyebrow" style={{ marginTop: 14 }}>Что вы получите</p>
      <h1>18 документов аудита</h1>
      <p className="sub" style={{ maxWidth: 620 }}>
        Диагностика в этом портале — вход в аудит Commerce OS™. На выходе — не презентация
        «с мнением», а рабочий пакет. <b>Большинство документов готовятся ещё до передачи
        доступов</b> — по опроснику и внешнему обходу.
      </p>

      <p className="eyebrow" style={{ margin: '24px 0 10px' }}>Ядро · отдаётся всегда</p>
      {CORE.map(([id, name, desc]) => (
        <div key={id} className="qcard" style={{ padding: 14 }}>
          <span className="qid">{id}</span>
          <p className="qtext" style={{ margin: '2px 0' }}>{name}</p>
          <p className="sub" style={{ fontSize: 12.5, margin: 0 }}>{desc}</p>
        </div>
      ))}

      <p className="eyebrow" style={{ margin: '24px 0 10px' }}>Приложения · по мере данных</p>
      <div className="grid cols2">
        {APPS.map(([id, name, desc]) => (
          <div key={id} className="card" style={{ padding: 14 }}>
            <span className="qid">{id}</span>
            <p style={{ fontWeight: 700, margin: '2px 0', fontSize: 14 }}>{name}</p>
            <p className="sub" style={{ fontSize: 12, margin: 0 }}>{desc}</p>
          </div>
        ))}
      </div>

      <div style={{ marginTop: 24 }}>
        <Link to="/company" className="btn">Начать диагностику →</Link>
      </div>
    </div>
  );
}
