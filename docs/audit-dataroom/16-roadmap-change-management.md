# 16 · Дорожная карта и управление изменениями

## N-03 · Git и ветвление (✅ заземлено)

- Репозиторий: `github.com/pyft4zym6y-cpu/Test`.
- ~348 коммитов, стиль **Conventional Commits** (`feat/fix/chore`), в основном на русском.
- Основная ветка `main`; активная работа — в ветках `claude/*` (текущая
  `claude/ai-assistant-comerc-oc-qufqcc`).
- **Нет git-тегов** и формального процесса релизов; версии в `package.json` статичны
  (`worker` 0.1.0, `portal` 1.0.0).

## N-02 · Версионирование и changelog (📝)

- Кодового CHANGELOG нет; есть методологический `portal/method/CHANGELOG.md` и content-round
  логи `worker/reference/weexp-os/_CHANGELOG_ROUND*.md`.
- **Рекомендация:** ввести семантическое версионирование + `CHANGELOG.md` на релиз, git-теги.

## N-01 · Дорожная карта продукта (📝)

Из состояния системы и реестра рисков вытекают ближайшие направления (предложение —
подтверждает владелец PO):

**Ближний горизонт (фундамент качества и безопасности):**
- Закрыть S-1/S-5 (токен движка, SSRF).
- Определить ретеншн `results/` + авто-очистка.
- Ввести vitest на чистые функции + CI-гейт (typecheck/тесты).
- ESLint/Prettier; `LICENSE`.

**Средний горизонт (комплаенс и масштаб):**
- DPA/SCC с субпроцессорами; RoPA; процедура DSAR.
- Threat model (STRIDE); план реагирования на инциденты; BCP/DR.
- Абстракция AI-провайдера (снижение vendor lock-in).

**Продукт:**
- Развитие worst-case реконструкции и сверки источников (раздел 09) — ключевой дифференциатор.
- Расширение реестра блоков/deliverables по мере валидации.

## F-05 · Управление изменениями (📝)

Формализовать: ветка → ревью → typecheck-гейт → деплой → откат. Сейчас деплой ручной
(`autoDeploy:false`), без обязательного гейта. Владелец: OPS + ENG.

## История изменений этого data room

Data room версионируется вместе с кодом. Каждый коммит отражает состояние системы на момент
коммита; статусы документов (✅/📝/⛔) обновляются по мере заполнения владельцами.
